--Capital Finder – Git Assignment
This project was created for the Individual Git Exercise.  
It demonstrates a simple Java program with two independent features, a testing component, and a full Git workflow including branching, merging, committing, pushing, and pull requests.

-- Program Overview
The program allows a user to:

1. Find the capital city of a country**  
2. List all supported countries**

It uses a small in-memory collection (HashMap) to store country–capital pairs.

--Features
--Feature 1: Get the capital of a country**
Method:
```java
public static String getCapital(String country)
